# requirements-cuda13.txt
# CUDA 13.0 compatible package versions
#
# IMPORTANT: Do NOT install with `pip install -r requirements-cuda13.txt`
# Instead use the installation script:
#   ./scripts/install_cuda13.sh
#
# Or install manually in order:
#   1. pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu130
#   2. pip install -r requirements-cuda13.txt
#
# This file is for reference and for installing non-PyTorch dependencies.

# Training libraries (from PyPI)
accelerate
deepspeed
transformers
peft

# Quantization
bitsandbytes
optimum-quanto

# Diffusers (specific commit for compatibility)
diffusers @ git+https://github.com/huggingface/diffusers@7a2b78bf0f788d311cc96b61e660a8e13e3b1e63

# Data & utilities
einops
huggingface-hub
datasets
omegaconf
sentencepiece
opencv-python
matplotlib
onnxruntime
timm
qwen_vl_utils
loguru

# FastSafeTensors
fastsafetensors

# Optional: Flash Attention for CUDA 13.0
# flash-attn  # Uncomment when available
# xformers   # Uncomment when available
